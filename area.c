#include<stdio.h>
int main()
{
int a,p,l,b;
printf("\n enter the length & width");
scanf("%d%d",&l,&b);
a=l*b;;
p=2*(l*b);
printf("\n the area is=%d",a);
printf("\n the parameter is=%d",p);

return 0;
}
