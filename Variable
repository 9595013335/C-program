
#include <stdio.h>

int a;             // Global Variable
void main()
{
    int b;        // Local Variable
   
   static int c;  // static Variable
   
   printf("\n %d",a);     // 0 value
   
   printf("\n %d",b);    // Garbage value
   
   printf("\n %d",c);   // 0 value

    return 0;
}
